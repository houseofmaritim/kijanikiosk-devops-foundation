apiVersion: v1
kind: Service
metadata:
  name: kk-payments
  namespace: kijani-project
spec:
  selector:
    app: kk-payments
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
